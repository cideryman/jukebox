# 나의 주크박스 설계 명세

> 이 문서는 후속 구현 에이전트(Antigravity 포함)를 위한 기준 문서다. 현재 코드가 단순하다는 이유로 아래 사용자 경험 계약을 임의로 변경하지 않는다.

## 1. 제품 목적

글자를 읽기 어려운 성인 지적장애인이 보호자의 도움 없이 자신이 듣고 싶은 음악을 직접 선택하는 태블릿 앱이다. 사용자가 사진을 누르면 즉시 결과가 발생해야 하며, 화면 이동·메뉴 탐색·문자 해독을 요구해서는 안 된다.

핵심 성공 기준은 “기능 수”가 아니라 다음 네 가지다.

1. 사용자가 원하는 음악을 스스로 선택할 수 있다.
2. 자신의 터치와 음악 재생 사이의 관계를 즉시 이해할 수 있다.
3. 실수로 설정이나 삭제 기능에 진입하기 어렵다.
4. 매번 같은 음악이 같은 위치에 있어 반복 학습이 가능하다.

## 2. 사용자와 디자인 태도

- 주 사용자는 성인이다. 유아용 장난감처럼 보이는 과도한 원색, 캐릭터 장식, 통통 튀는 애니메이션을 사용하지 않는다.
- 장애 유형과 정도는 개인마다 다르므로 “지적장애인은 모두 같은 방식으로 사용한다”는 가정을 하지 않는다.
- 사용 화면과 보호자 화면을 분리한다. 사용 화면은 문자 없이 선택에만 집중하고, 보호자 화면에는 필요한 설명과 파일명을 표시한다.
- 색상만으로 상태를 전달하지 않는다. 재생 상태는 강조 테두리와 정지 도형을 함께 사용한다.
- 사용자가 누르지 않은 음악을 자동으로 재생하지 않는다.

## 3. 범위

### 현재 목업에 포함

- 가로·세로 회전에 대응하는 3열×3행 고정 격자
- 로컬 음원 및 썸네일을 최대 9개 슬롯에 현재 세션 동안 등록
- 단일 음원 재생, 동일 카드 정지, 다른 카드 즉시 전환
- 2초 길게 누르는 보호자 설정 진입
- Android Chrome의 Media Session 및 PWA 앱 셸 오프라인 캐시
- 키보드 포커스, 스크린리더 이름, 모션 감소 설정

### 현재 목업에서 제외

- 새로고침 이후 파일 보존
- 서버 업로드, 계정, 동기화
- PIN, 사용자 프로필, 4×4 격자
- 자동 다음 곡, 셔플, 반복, 재생목록
- 음량 조절, 탐색 막대, 곡명 표시
- iOS 백그라운드 재생 보장

## 4. 변경하면 안 되는 사용자 경험 계약

### 재생 규칙

| 현재 상태 | 사용자 입력 | 결과 |
|---|---|---|
| 정지 | A 카드 터치 | A를 처음부터 재생 |
| A 재생 | A 카드 터치 | A 정지, 재생 위치를 처음으로 초기화 |
| A 재생 | B 카드 터치 | A 즉시 정지 후 B를 처음부터 재생 |
| 재생 | 곡 끝남 | 선택 없는 정지 상태로 복귀 |
| 로딩 | 다른 카드 터치 | 이전 요청을 무효화하고 마지막 카드만 재생 |

- 카드 터치로 다른 페이지나 상세 화면으로 이동하지 않는다.
- 곡이 끝나도 다음 곡을 자동으로 재생하지 않는다.
- 동시에 두 음원이 재생되지 않도록 `<audio>` 요소는 하나만 사용한다.
- 화면에서의 “정지”는 `pause()` 후 `currentTime = 0`을 의미한다.
- 시스템 잠금 화면의 일시정지는 재개 가능하도록 내부적으로 `paused` 상태를 허용한다. 사용 화면에는 재생 중 표시를 남기지 않는다.
- 첫 카드 입력은 즉시 실행하고 이후 700ms 안의 반복 입력은 무시한다. 반복 입력이 계속되면 마지막 입력부터 안정화 시간을 다시 계산하며, 700ms 이상 멈춘 뒤의 같은 카드 입력은 정상 정지로 처리한다.

### 설정 진입

- 오른쪽 위 톱니의 시각적 크기는 약 18~20px이지만 실제 터치 영역은 최소 48×48px이다.
- 포인터나 Enter/Space 키를 2,000ms 동안 계속 누른 경우에만 설정을 연다.
- 누르는 도중 손가락이 벗어나거나 취소·해제되면 타이머를 취소한다.
- 짧게 누르면 설정을 열지 않고 보호자 안내만 잠시 표시한다.
- 설정은 모달 오버레이로 열며 재생 카드 터치 흐름에는 화면 전환이 없다.

### 슬롯 등록

- 슬롯 ID는 1부터 9까지 고정하며 자동 정렬하지 않는다.
- 음원과 썸네일이 모두 있어야 사용자 화면에 카드를 표시한다.
- 빈 슬롯은 격자 위치를 차지하지만 `visibility: hidden`으로 사용자에게 보이지 않고 눌리지 않는다.
- 기존 슬롯의 음원 또는 사진만 교체할 수 있다. 재생 중인 슬롯을 수정하거나 삭제하기 전에는 재생을 중지한다.
- 삭제에는 보호자 확인 단계를 둔다.

## 5. 시각 시스템

### 방향

- 차분한 짙은 남색 배경과 사실적인 사진 중심의 성인용 디자인
- 사용자 화면에는 헤더, 하단 메뉴, 곡명, 페이지 표시를 두지 않는다.
- 3×3 격자는 안전 영역을 제외한 화면 전체를 사용한다.
- 가로와 세로 모두 3열×3행을 유지하므로 카드는 화면 비율에 따라 가로형 또는 세로형이 될 수 있다. 썸네일은 `object-fit: cover`로 채운다.

### 기준 토큰

| 역할 | 값 |
|---|---|
| 배경 | `#0b1220` |
| 설정 패널 | `#111b2e` |
| 카드/입력 표면 | `#18243a` |
| 기본 글자 | `#f5f8ff` |
| 보조 글자 | `#aab7cb` |
| 재생 강조 | `#58e6d2` |
| 오류 | `#ff918c` |
| 카드 간격 | `clamp(8px, 1.2vmin, 16px)` |
| 카드 모서리 | `clamp(16px, 2.2vmin, 28px)` |
| 재생 테두리 | 6px 강조색 |

### 상태 표현

- 재생: 청록색 6px 테두리 + 중앙의 반투명 원형 표면 + 흰색 정지 사각형
- 로딩: 같은 테두리 + 중앙 회전 표시. `prefers-reduced-motion`에서는 회전을 사실상 제거한다.
- 오류: 짧은 붉은 테두리 + 보호자용 토스트. 오류가 선택 상태로 남지 않게 한다.
- 누름: 120ms 이내의 아주 작은 축소만 허용한다. 반복·장식 애니메이션은 금지한다.

## 6. 상태와 데이터 계약

현재 구현 모델은 다음 의미를 가진다.

```ts
type SlotId = 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9;

interface TrackSlot {
  id: SlotId;
  audioFile: File | null;
  audioUrl: string | null;
  imageFile: File | null;
  imageUrl: string | null;
  label: string; // 보호자 화면과 Media Session에서만 사용
}

type PlaybackStatus = "idle" | "loading" | "playing" | "paused" | "error";

interface PlaybackState {
  status: PlaybackStatus;
  activeSlotId: SlotId | null;
  requestId: number;       // 오래된 play() 결과 무효화
  errorSlotId: SlotId | null;
}
```

핵심 동작의 책임은 다음과 같이 유지한다.

- `registerFile(slotId, kind, file)`: 파일 형식 확인, 이전 URL 해제, 새 오브젝트 URL 생성
- `clearSlot(slotId)`: 재생 중지, URL 해제, 슬롯 초기화
- `playSlot(slotId)`: 이전 요청 무효화, 단일 오디오 소스 교체, 재생 상태 반영
- `stopPlayback()`: 정지 및 처음으로 되감기, 선택 상태 해제
- `renderJukebox()` / `renderSettings()`: 상태로부터 DOM을 다시 생성

후속 구현에서도 UI가 저장소에 직접 의존하지 않도록 저장 계층을 별도 모듈로 추가한다.

```ts
interface JukeboxStorage {
  listSlots(): Promise<PersistedSlot[]>;
  saveSlot(slotId: SlotId, audio: File, image: File): Promise<PersistedSlot>;
  removeSlot(slotId: SlotId): Promise<void>;
  estimate(): Promise<{ usage: number; quota: number; persisted: boolean }>;
}
```

## 7. Antigravity 후속 구현 지시

### 우선순위 1: 영구 로컬 저장

1. 슬롯 메타데이터는 IndexedDB, 음원과 썸네일 바이너리는 OPFS에 저장한다.
2. 저장 파일명에는 슬롯 번호와 생성한 UUID를 사용하고 원본 파일명에 의존하지 않는다.
3. 앱 시작 시 1~9번 슬롯을 복원하고 존재하지 않거나 손상된 파일은 해당 슬롯만 `error`로 표시한다.
4. `navigator.storage.persist()`를 요청하고 설정 화면에 사용량·전체 허용량·영구 저장 허용 여부를 표시한다.
5. 브라우저 데이터 삭제 시 파일도 삭제될 수 있음을 보호자 화면에 명시한다.
6. 저장 계층이 실패해도 사용자 화면이 무한 로딩 상태에 머물지 않게 한다.

### 우선순위 2: 백업과 복원

- 9개 슬롯의 메타데이터, 음원, 이미지, 앱 설정과 재생 통계를 한 개의 ZIP 백업 파일로 내보내고 다시 가져올 수 있게 한다.
- 복원 전 기존 데이터가 교체됨을 확인하고, 불완전한 백업은 기존 데이터를 변경하지 않는 트랜잭션 방식으로 처리한다.
- 백업 형식 버전을 포함한다. 최초 버전은 `jukebox-backup-v1`로 한다.

### 우선순위 3: 현장 검증 뒤 확장

- 4×4는 코드 상수만 바꾸는 방식이 아니라 실제 사용자 평가 후 보호자 설정 옵션으로 추가한다.
- 사용자 프로필은 같은 태블릿을 여러 사람이 공유해야 한다는 요구가 확인된 경우에만 추가한다.
- PIN은 길게 누르기만으로 설정 오진입이 발생한다는 관찰 결과가 있을 때 추가한다.

### 추가하지 말아야 할 것

- 사용자 화면의 내비게이션, 검색, 추천, 재생목록, 텍스트 라벨
- 자동 정렬, 최근 재생 순 정렬, 무작위 위치 변경
- 사용자 터치 없이 시작하는 자동재생
- 디자인을 풍성하게 보이기 위한 장식 카드, 배지, 통계
- 실제 사용자 검증 없이 “장애인용”이라는 이유로 유아적인 표현을 추가하는 것

## 8. 오류 및 경계 조건

- 사용자가 파일 선택을 취소하면 기존 슬롯을 변경하지 않는다.
- MIME 유형이 `audio/` 또는 `image/`로 시작하지 않으면 등록하지 않는다.
- `play()`가 거부되거나 중단된 경우 가장 최근 요청에 대해서만 오류를 표시한다.
- A 재생 요청이 끝나기 전에 B를 누르면 A의 Promise 완료가 B를 멈추거나 상태를 덮어쓰지 못한다.
- 파일 URL을 교체·삭제하거나 페이지를 떠날 때 `URL.revokeObjectURL()`을 호출한다.
- Media Session, Service Worker 또는 영구 저장 API가 없어도 기본 화면과 터치 재생은 계속 동작해야 한다.
- 세로/가로 회전 중에도 활성 슬롯 ID와 재생은 유지한다.

## 9. 접근성 요구사항

- 모든 동작 요소는 실제 `button`, `input`, `dialog` 요소를 사용한다.
- 카드의 접근성 이름은 상태에 따라 “1번 음악 재생”과 “1번 음악 정지”로 갱신한다.
- 카드 터치 영역은 카드 전체이며 설정 버튼을 포함한 작은 조작도 최소 48×48px로 한다.
- 모든 키보드 조작에 명확한 포커스 표시를 제공한다.
- 사용자 화면의 상태 변화는 `aria-live`로 알리되 화면에 텍스트를 강제로 표시하지 않는다.
- 설정 화면의 글자와 배경은 WCAG AA 대비 이상을 유지한다.
- `prefers-reduced-motion: reduce`를 존중한다.
- 햅틱 진동은 플랫폼별 동작 차이와 감각 과민 가능성이 있으므로 기본값으로 추가하지 않는다.

## 10. 완료 검증 체크리스트

### 현재 목업

- [ ] 1280×800과 800×1280에서 스크롤 없이 3×3 격자가 유지된다.
- [ ] 짧은 톱니 터치로는 설정이 열리지 않고 2초 누르기에서만 열린다.
- [ ] MP3/M4A와 JPG/PNG를 서로 다른 슬롯에 등록할 수 있다.
- [ ] 음원과 사진이 모두 등록된 슬롯만 사용자 화면에 나타난다.
- [ ] 동일 카드 재터치는 정지하며, 다른 카드 터치는 한 음원만 재생한다.
- [ ] 곡이 끝나면 재생 강조가 사라진다.
- [ ] 빠르게 여러 카드를 눌러도 마지막 선택만 재생된다.
- [ ] 화면 회전, 설정 열기/닫기 중 재생이 불필요하게 중단되지 않는다.
- [ ] 첫 온라인 접속 후 앱 셸은 오프라인에서도 열린다.
- [ ] 새로고침 시 파일이 초기화되고 보호자에게 목업 제한이 표시된다.

### 영구 저장 단계

- [ ] 앱 종료와 태블릿 재부팅 후에도 9개 슬롯이 복원된다.
- [ ] 파일 교체·삭제 시 사용하지 않는 OPFS 파일이 남지 않는다.
- [ ] 저장 공간 부족·권한 거부·손상 파일이 다른 슬롯의 사용을 막지 않는다.
- [ ] 백업을 다른 동일 앱 설치에 복원할 수 있다.
- [ ] Android 실제 기기의 홈 화면 설치, 화면 잠금, 블루투스 출력에서 시험한다.

## 11. 현장 사용자 평가

기능 완료 판단은 개발자 테스트만으로 하지 않는다. 실제 사용자 2~3명과 보호자가 다음 항목을 관찰한다.

- 원하는 사진을 스스로 찾고 한 번의 터치로 재생하는가
- 재생 중인 카드가 무엇인지 구별하는가
- 같은 카드를 다시 눌러 멈출 수 있는가
- 빈 공간이나 설정 버튼을 반복해서 잘못 누르는가
- 카드 위치를 다음 사용 때 기억하는가
- 소리와 화면 반응이 너무 빠르거나 느리게 느껴지지 않는가

관찰 결과를 근거로 간격, 강조 표시, 설정 보호 시간을 조정하되 핵심 재생 규칙과 고정 위치 원칙은 유지한다.
