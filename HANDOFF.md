# 주크박스 후속 작업 핸드오프

## 1. 제품 목적과 기준 문서

이 앱은 글자를 읽기 어려운 성인 지적장애인이 태블릿에서 사진을 한 번 눌러 원하는 음악을 직접 선택하는 로컬 전용 PWA다.

- 제품·접근성·재생 동작의 최상위 기준은 `DESIGN.md`다.
- 사용자 화면에는 새 메뉴, 곡명, 탐색 화면을 추가하지 않는다.
- `DESIGN.md`에서 제외한 음량 조절은 사용자 화면의 조작을 뜻한다. 이 핸드오프는 보호자 설정의 최대 음량 상한만 예외적으로 추가한다.
- 아래 작업은 보호자 설정과 저장 계층을 확장하되 기존의 3×3 고정 위치와 한 번 터치 재생 계약을 바꾸지 않는다.
- `walkthrough/1.md`와 `walkthrough/2.md`는 작업 보고서일 뿐 검증의 기준으로 간주하지 않는다. 실제 코드와 독립된 테스트로 다시 확인한다.

## 2. 현재 저장소 상태

- 작업 브랜치: `main`
- 영구 저장 구현 커밋: `44d37d4`
- 1차 워크스루 커밋: `af6fb19`
- Antigravity의 2차 자체 검토 변경과 Codex의 데이터 안전 수정은 현재 코드에 통합했다.
- 기존 `WALKTHROUGH.md`는 `walkthrough/1.md`로 이동했으며, 후속 결과는 번호를 붙여 보관한다.
- 데이터 안전 수정은 `walkthrough/3.md`, 모바일 파일 등록 수정은 `walkthrough/4.md`, 설정 종료 UI 변경은 `walkthrough/5.md`, 파일 내용 판별 보완은 `walkthrough/6.md`, 앨범아트·최대 음량 구현은 `walkthrough/7.md`, 백업 기능 우선순위 설계는 `walkthrough/8.md`, 반복·긴 입력 대응 설계는 `walkthrough/9.md`, 재생 통계 설계는 `walkthrough/10.md`, 우선순위 전체 구현 결과는 `walkthrough/11.md`를 확인한다.

후속 작업자는 기존 변경을 초기화하거나 덮어쓰지 말고 현재 작업 트리부터 확인한다.

## 3. 작업 순서

### 완료된 1단계: 병합 전 데이터 안전 문제 수정

다음 문제는 앨범아트와 최대 음량 구현 전에 수정했다. 후속 작업에서도 회귀시키지 않는다.

1. [x] `test.html`을 운영 저장소에서 완전히 격리한다.
   - 현재 테스트는 앱과 같은 `jukebox_db`와 OPFS `media/`를 사용하며 슬롯 1~4를 덮어쓰기·삭제·손상시킨다.
   - 테스트용 DB 이름과 OPFS 디렉터리를 주입할 수 있게 저장 클래스를 구성한다.
   - 테스트 전후 데이터를 정리하고, 같은 페이지를 여러 번 실행해도 같은 결과가 나와야 한다.
2. [x] IndexedDB 쓰기·삭제는 요청 성공이 아니라 트랜잭션의 `oncomplete` 이후 성공 처리한다.
   - `onerror`와 `onabort`를 모두 실패로 처리한다.
   - 커밋이 확정되기 전에 이전 OPFS 파일을 삭제하지 않는다.
3. [x] `clearSlot()`에서 영구 삭제가 실패하면 성공으로 안내하거나 화면만 비우지 않는다.
   - 기존 슬롯을 유지하고 보호자에게 재시도 가능한 오류를 표시한다.
4. [x] `storage.init()`은 공유하는 단일 `initPromise`로 직렬화한다.
   - 복원과 저장이 동시에 시작돼도 고아 파일 정리가 저장 중인 새 파일을 삭제하지 않아야 한다.
5. [x] 음원 파일을 교체하면 보호자 화면 및 Media Session의 `label`도 새 음원명으로 갱신한다.
6. [x] 서비스 워커 테스트를 실제 검증으로 교체한다.
   - 서비스 워커 등록과 활성화 완료를 기다린다.
   - 현재 셸 캐시와 `storage.js` 등 필수 파일의 존재를 확인한다.
   - 온라인 최초 실행 후 오프라인 새로고침을 별도 브라우저 시나리오로 검증한다.

### 완료된 2단계: MP3 내장 앨범아트 기본 사용

- [x] ID3v2.3/v2.4 `APIC` 프레임의 JPEG·PNG·GIF·WebP 이미지를 로컬에서 추출한다.
- [x] 수동 이미지가 내장 이미지보다 우선하며, 음원 교체 시 출처에 맞춰 유지·교체·제거한다.
- [x] 추출한 이미지를 OPFS와 IndexedDB에 저장하고 재시작 후 복원한다.
- [x] 추출 실패 또는 앨범아트 없음은 음원 등록을 막지 않는다.

#### 사용자 경험

- 보호자가 MP3 등 음원 파일을 선택하면 파일에 내장된 앨범아트를 먼저 찾는다.
- 내장 앨범아트가 있으면 별도의 사진 선택 없이 그 이미지를 해당 슬롯의 기본 썸네일로 사용한다.
- 보호자가 사진을 직접 선택하면 내장 앨범아트보다 직접 선택한 사진을 우선 사용한다.
- 내장 앨범아트도 없고 직접 선택한 사진도 없으면 현재 계약대로 슬롯은 사용자 화면에 나타나지 않는다.
- 앨범아트 추출 실패가 음원 등록 자체를 막아서는 안 된다. 보호자 설정에는 음악이 등록되고 사진이 필요하다는 상태를 표시한다.

#### 이미지 우선순위와 교체 규칙

썸네일 선택 우선순위는 다음과 같다.

1. 보호자가 직접 선택한 이미지(`manual`)
2. 현재 음원에서 추출한 내장 앨범아트(`embedded`)
3. 이미지 없음

- 슬롯 메타데이터에 `imageSource: "manual" | "embedded" | null`을 저장한다.
- 직접 이미지를 선택하면 `imageSource`를 `manual`로 저장하고 기존 내장 앨범아트를 대체한다.
- 음원을 교체할 때 현재 이미지가 `manual`이면 그 이미지를 유지한다.
- 음원을 교체할 때 현재 이미지가 `embedded`이면 이전 음원의 이미지를 제거하고 새 음원의 앨범아트로 교체한다.
- 새 음원에 앨범아트가 없으면 이전 음원에서 추출한 이미지를 남겨 잘못된 사진을 보여주지 않는다.
- 추출한 이미지는 새로고침과 앱 재시작 후에도 보이도록 OPFS에 저장하고 IndexedDB 메타데이터와 연결한다.
- 파서 라이브러리를 사용한다면 CDN에 의존하지 말고 앱 셸과 함께 로컬에 포함해 오프라인에서도 작동하게 한다.
- 최소 지원 대상은 ID3v2의 `APIC` 프레임이다. 가능하면 M4A/MP4의 `covr`도 점진적으로 지원하되, MP3 지원 완료를 지연시키지 않는다.

#### 저장 안전성

- 음원과 추출 이미지의 저장은 기존 안전 저장 순서를 따른다.
- 새 음원 저장 또는 메타데이터 커밋이 실패하면 기존 음원·수동 이미지·내장 이미지를 보존한다.
- 교체가 완전히 성공한 뒤에만 이전 음원과 이전 내장 이미지를 삭제한다.

### 완료된 3단계: 보호자용 최대 음량 설정

- [x] 보호자 설정에 10%~100%, 10% 단위 슬라이더와 현재 값을 추가한다.
- [x] 변경 즉시 `<audio>`에 적용하고 IndexedDB에 영구 저장한다.
- [x] DB v1에서 v2로 올릴 때 기존 슬롯을 보존하고 `settings` 스토어를 추가한다.
- [x] 손상되거나 범위를 벗어난 값은 100%로 복구한다.

#### 의미와 한계

- 최대 음량은 기기의 시스템 음량이나 블루투스 스피커 자체를 잠그는 기능이 아니다.
- 웹앱에서 재생하는 `<audio>` 요소의 출력 비율을 제한하는 앱 내부 상한이다.
- 기기 물리 버튼으로 시스템 음량을 올리면 실제 소리는 여전히 커질 수 있다는 설명을 보호자 설정에 짧게 표시한다.

#### 동작

- 보호자 설정에 `최대 음량` 항목을 추가한다.
- 기본값은 `100%`, 선택 범위는 `10%~100%`, 변경 단위는 `10%`로 한다.
- 접근 가능한 `input type="range"`와 현재 백분율 표시를 사용한다.
- 값은 로컬 저장소에 영구 저장하여 앱 재시작 후에도 유지한다.
- 재생 직전과 설정 변경 직후 `audio.volume = maxVolume / 100`을 적용한다.
- 재생 중 값을 낮추면 즉시 반영한다.
- 최대 음량 설정은 2초 길게 누르는 보호자 설정 안에서만 변경할 수 있다.
- 사용자 화면에는 음량 버튼, 수치 또는 슬라이더를 추가하지 않는다.
- 저장된 값이 없거나 손상됐으면 `100%`로 복구해 기존 동작을 유지하고 앱 재생을 막지 않는다.

#### 저장 모델 권장안

IndexedDB에 별도의 `settings` 오브젝트 스토어를 추가하고 다음 값을 저장한다.

```ts
interface JukeboxSettings {
  key: "app-settings";
  maxVolume: number; // 10~100
}
```

- DB 버전 업그레이드 시 기존 `slots` 데이터가 유지되어야 한다.
- 구버전에서 새 버전으로 처음 실행했을 때 기존 음악과 이미지가 모두 복원되는지 마이그레이션 테스트를 추가한다.

### 완료된 4단계: 전체 주크박스 백업 및 복원

#### 목적과 범위

- 브라우저의 사이트 데이터 또는 설치된 PWA의 앱 데이터를 초기화해 IndexedDB와 OPFS가 함께 지워져도 다시 복원할 수 있게 한다.
- 일반 서비스 워커 캐시 갱신만으로는 등록 음악이 삭제되지 않는다는 점을 보호자에게 명확히 안내한다.
- 백업 파일은 기기의 다운로드·파일 앱 등 PWA 저장소 바깥에 보관한다.
- 서버 업로드 없이 기기 안에서만 내보내고 가져온다.

#### 백업할 데이터

- 1~9번 슬롯 위치와 음원 원본
- 수동 이미지 또는 추출된 내장 앨범아트
- `imageSource`, 파일명, MIME, 슬롯 표시 이름 등 복원에 필요한 메타데이터
- 보호자 최대 음량을 포함한 앱 설정
- 재생 통계 기능 구현 이후에는 곡별 재생 횟수·완료 횟수·누적 청취 시간도 포함한다.
- 백업 형식 버전, 생성 시각, 각 파일의 크기와 SHA-256 체크섬
- 일시적인 Blob URL, OPFS 내부 경로, 재생 위치와 같은 기기 종속·세션 데이터는 제외한다.

#### 사용자 경험

- 2초 길게 누르는 보호자 설정 안에 `전체 백업 저장`과 `백업에서 복원`을 둔다.
- 하나의 버전 지정 `.jukebox-backup.zip` 파일로 내보내 Android 다운로드 폴더와 iPhone 파일 앱에서 찾기 쉽게 한다.
- 백업 중 진행 상태와 예상 용량을 표시하고 완료 전에는 설정을 닫지 못하게 한다.
- 복원 전에 백업 날짜, 음악 수, 전체 크기를 먼저 보여주고 기존 내용을 교체한다는 확인을 받는다.
- 손상 파일, 지원하지 않는 버전, 용량 부족은 구체적으로 안내하며 기존 주크박스는 그대로 유지한다.

#### 데이터 안전 구현 원칙

- ZIP 처리 코드는 CDN이 아니라 앱 셸에 로컬 포함하며 가능하면 스트리밍 방식으로 큰 음원의 메모리 사용을 제한한다.
- 가져오기 시 경로 순회(`../`), 과도한 압축 해제 크기, 중복 슬롯, MIME 위조를 검증한다.
- 모든 파일과 체크섬을 먼저 임시 영역에서 검증한 뒤, 새 메타데이터 커밋이 성공한 경우에만 기존 슬롯을 교체한다.
- 복원 중 실패·앱 종료·저장소 부족이 발생해도 기존 음원과 사진을 삭제하지 않는다.
- 백업 형식은 `formatVersion`을 두어 향후 슬롯 수나 설정이 늘어나도 이전 백업을 읽을 수 있게 한다.

### 완료된 5단계: 반복 터치·긴 누르기 입력 안정화

#### 목표

- 사용자가 짧은 시간에 같은 카드를 여러 번 눌러도 음악이 재생과 정지를 빠르게 반복하지 않게 한다.
- 음악 카드를 오래 누르거나 손을 떼는 데 시간이 걸려도 한 번의 선택으로만 처리한다.
- 첫 입력의 반응은 늦추지 않고 즉시 재생해 인과관계를 이해하기 쉽게 유지한다.
- 보호자 설정의 톱니바퀴 2초 길게 누르기 규칙은 음악 카드 규칙과 분리한다.

#### 권장 입력 규칙

- 첫 번째 유효한 카드 선택은 즉시 실행한다.
- 실행 직후 기본 700ms의 `입력 안정화 구간`을 시작하고, 이 구간의 추가 터치는 실행하지 않는다.
- 안정화 구간에 추가 터치가 들어오면 마지막 입력부터 다시 700ms를 계산한다. 따라서 계속 두드리는 동안에는 첫 동작만 유지한다.
- 터치가 700ms 이상 멈춘 뒤 같은 카드를 한 번 누르면 기존 계약대로 정지한다.
- 안정화 구간에는 다른 카드 선택도 무시해 반복 입력 중 우연히 음악이 계속 바뀌지 않게 한다.
- 음악 카드의 긴 누르기는 누르는 동안 시각적 눌림 상태만 보여주고, 같은 카드에서 손을 뗐을 때 정확히 한 번 실행한다.
- 손가락이 카드 밖으로 크게 이동하거나 `pointercancel`이 발생하면 실행하지 않는다.
- 보조 손가락·다중 터치·마우스 보조 버튼은 무시하고, 하나의 주 포인터만 추적한다.
- 브라우저의 이미지 끌기, 텍스트 선택, 긴 누르기 컨텍스트 메뉴가 카드 조작을 방해하지 않게 한다.
- 키보드 Enter·Space와 보조공학 입력은 유지하되 `event.repeat`로 발생한 반복 키 입력은 한 번으로 제한한다.

#### 피드백과 조정

- 안정화 중인 카드는 짧고 분명한 테두리·밝기 변화로 선택되었음을 보여주며 글자 안내에 의존하지 않는다.
- 무시한 반복 터치마다 토스트나 경고음을 내지 않는다. 과도한 피드백은 사용자를 더 자극할 수 있다.
- 700ms는 초기값이며 실제 이용자와 보호자 관찰을 통해 500~1000ms 범위에서 조정한다.
- 개인정보 보호를 위해 입력 패턴을 서버로 전송하지 않고 실기기 관찰 테스트로 조정한다.

### 완료된 6단계: 보호자용 재생 통계

#### 목적과 표시 원칙

- 이용자가 스스로 자주 선택하는 음악을 보호자가 이해하고 지원할 수 있게 한다.
- 통계는 2초 길게 누르는 보호자 설정에서만 보여주고 사용자용 3×3 화면에는 숫자·순위·그래프를 추가하지 않는다.
- 슬롯 위치는 익숙함을 위해 고정하며 재생 횟수에 따라 자동 재정렬하지 않는다.
- 통계를 목표 달성, 능력 평가 또는 선호를 강요하는 근거로 사용하지 않고 선택 경향을 참고하는 정보로만 표현한다.
- 모든 통계는 기기 안에만 저장하고 서버·분석 서비스로 전송하지 않는다.

#### 집계 기준

- 반복 터치 안정화 규칙을 통과하고 `audio.play()`가 실제 성공한 새로운 카드 선택만 `선택 재생 횟수`에 1회 기록한다.
- 재생 실패, 안정화 구간에서 무시된 터치, 같은 카드 정지 동작은 횟수에 포함하지 않는다.
- 잠금 화면이나 Media Session에서 일시정지 후 이어듣기는 새 선택으로 세지 않는다.
- 곡이 끝까지 재생되어 `ended` 이벤트가 발생한 경우에만 `완료 재생 횟수`를 올린다.
- 실제 재생 상태였던 시간만 `누적 청취 시간`에 더하고 일시정지·로딩·오류 시간은 제외한다.
- 마지막으로 재생에 성공한 시각을 `최근 재생`으로 저장한다.

#### 곡과 통계의 연결

- 음원을 등록할 때 고유한 `trackId`를 만들고 통계는 슬롯 번호가 아니라 `trackId`에 연결한다.
- 사진만 교체하면 같은 곡의 통계를 유지한다.
- 음원을 교체하거나 슬롯을 비우면 이전 곡 통계를 새 곡에 물려주지 않는다.
- 기존 설치의 음원에는 DB 업그레이드 시 `trackId`를 안전하게 부여하고 파일은 그대로 보존한다.
- 통계 저장 실패가 음악 재생을 막아서는 안 되며 다음 저장 기회에 재시도한다.

#### 보호자 화면

- 각 슬롯의 사진과 함께 `선택 횟수`, `완료 횟수`, `누적 청취 시간`, `최근 재생`을 간단히 표시한다.
- 값이 없는 경우 `0회`와 `기록 없음`으로 명확히 표시한다.
- 슬롯별 기록 초기화와 전체 기록 초기화를 제공하고 각각 별도 확인을 받는다.
- 향후 기간별 통계가 필요해질 때까지 첫 버전은 전체 누적 통계만 제공해 데이터와 화면 복잡도를 제한한다.

#### 저장 안정성

- 별도 IndexedDB `stats` 스토어를 추가하되 기존 `slots`와 `settings` 데이터가 유지되도록 DB 마이그레이션한다.
- 재생 중에는 메모리에 누적하고 일시정지·정지·곡 변경·종료 시 저장하며, 긴 재생은 일정 간격으로 체크포인트를 남긴다.
- 앱이 비정상 종료되더라도 손실되는 청취 시간이 체크포인트 간격을 넘지 않게 한다.
- 통계는 전체 백업·복원 대상에 포함하며 형식 버전이 오래된 백업에는 통계가 없어도 정상 복원한다.

### 완료된 7단계: 앱 아이콘 단순화 및 현대화

- 현재 레코드판·음표 조합을 더 단순하고 세련된 한 가지 상징으로 정리한다.
- 글자를 읽지 못하는 사용자도 주크박스 앱임을 구분할 수 있도록 형태 인지성을 최우선으로 한다.
- 작은 홈 화면 아이콘에서도 뭉개지지 않도록 선과 요소 수를 줄이고 명암 대비를 높인다.
- 기존 청록색 포인트와 짙은 배경을 유지해 앱 화면과 같은 브랜드로 보이게 한다.
- Android 마스커블 아이콘 안전 영역을 지키고 SVG 원본과 192×192, 512×512 PNG를 제공한다.
- iPhone 홈 화면용 `apple-touch-icon`도 함께 연결한다.
- 최소 2~3개 시안을 실제 태블릿 홈 화면 크기로 비교한 뒤 최종안을 선택한다.

### 예정된 8단계 (우선순위 1): 음악 재생 중 화면 꺼짐 방지 (Screen Wake Lock)

#### 목적과 필요성
- 음악이 재생되는 도중 태블릿 화면이 자동으로 꺼지면 이용자가 음악이 멈춘 것으로 오해하거나 다시 조작하기 어려워지는 문제를 해결한다.
- 웹 표준 `Screen Wake Lock API`를 활용하여 앱 실행 및 음악 재생 시 화면 켜짐 상태를 스마트하게 유지한다.

#### 세부 동작 및 보호자 설정
- **기본 동작**: 음악 카드를 눌러 음악이 재생되기 시작하면 자동으로 화면 꺼짐 방지(`wakeLock.request('screen')`)를 활성화한다.
- **해제 조건**: 음악이 끝나거나 이용자가 정지하면 잠금 방지를 해제하여 태블릿 배터리를 보존한다.
- **보호자 설정 항목**:
  - `화면 켜짐 유지`: [재생 중에만 켜짐 (기본값) / 항상 켜짐 / 5분 유지 / 기기 기본 설정 따름]
- **예외 복구**: 브라우저 탭 전환, 백그라운드 복귀 시 `visibilitychange` 이벤트를 감지하여 Wake Lock을 안전하게 재획득한다.
- API를 지원하지 않는 구형 브라우저 환경에서도 에러 없이 정상 재생되도록 안전한 fallback을 둔다.

### 예정된 9단계 (우선순위 2): 타일 터치 영역 및 화면 그리드 모드 최적화

#### 목적과 접근성 개선
- 성인 지적장애인 및 뇌병변 장애인의 소근육 조절 능력과 시각 집중도에 맞춰 타일의 크기를 대폭 키우고 터치 오작동을 줄인다.
- 사용자 화면의 불필요한 테두리 여백(gap/padding)을 줄여 실제 터치 가능 면적을 극대화한다.

#### 세부 구성
1. **보호자 설정의 그리드 모드 선택**:
   - `2×2 모드 (4곡)`: 타일 하나가 화면 1/4 크기로 아주 큼직하게 표시되어 중증 장애인에게 최적.
   - `2×3 모드 (6곡)`: 중간 크기 및 균형 잡힌 선택지.
   - `3×3 모드 (9곡)`: 기존 기본값.
2. **Edge-to-Edge 여백 최적화**:
   - 화면 해상도에 맞게 타일 간 간격을 줄이고 버튼 패딩을 채워 터치 히트박스를 최대화.
3. **사용자 화면 불변 원칙**:
   - 사용자 화면에는 그리드 전환 버튼이나 UI 요소를 일절 두지 않으며, 오직 보호자 설정에서만 변경할 수 있다.

### 예정된 10단계 (우선순위 3): 보호자 설정 기반 다중 화면 세트 (화면 1, 2, 3) 관리

#### 목적과 사용자 경험
- 사용자 플레이 화면은 여전히 단일 화면(조작 요소 0)으로 유지하되, 보호자가 활동 목적(예: 오전 활동, 휴식, 개별 이용자 전용)에 따라 곡 세트를 교체할 수 있게 한다.
- 이용자에게 페이지 넘김이나 스와이프 같은 인지 부하를 강요하지 않는다.

#### 세부 설계
1. **저장 구조 확장**:
   - IndexedDB의 슬롯 관리를 `screenId: 1 | 2 | 3`으로 분리하여 각 화면별 슬롯 및 그리드 모드를 독립적으로 저장.
   - 현재 활성화된 화면 번호는 `settings`에 영구 보관.
2. **기존 기능 연동 및 안전성**:
   - **재생 통계**: 통계는 곡 고유의 `trackId`에 종속되므로 화면 전환 시에도 누적 통계가 훼손되지 않음.
   - **백업 및 복원**: 전체 백업 ZIP 파일 포맷(`formatVersion` 상향)에 1~3번 화면 전체 데이터를 담고, 구버전 백업 파일도 1번 화면으로 완벽히 하위 호환 복원되도록 처리.

## 6. 필수 자동화 검증

### 저장소와 회귀

- 테스트가 실제 `jukebox_db`와 `media/`를 사용하지 않는다.
- 같은 테스트를 같은 출처에서 연속 두 번 실행해도 모두 통과한다.
- IDB 트랜잭션 중단을 강제로 발생시켜도 기존 슬롯 파일이 보존된다.
- 삭제 실패 시 화면과 영구 저장 상태가 서로 다르게 표시되지 않는다.
- 앱 시작 직후 복원과 파일 등록을 겹쳐 실행해도 새 파일이 사라지지 않는다.

### 앨범아트

- 앨범아트가 포함된 MP3를 등록하면 사진을 따로 등록하지 않아도 카드가 나타난다.
- 앨범아트가 없는 MP3는 음악만 등록되고 사진 선택 전에는 카드가 나타나지 않는다.
- 직접 선택한 사진이 내장 앨범아트보다 우선한다.
- 수동 사진이 있는 슬롯의 음원을 교체해도 수동 사진이 유지된다.
- 내장 이미지를 쓰는 슬롯의 음원을 교체하면 새 앨범아트로 바뀐다.
- 새 음원에 앨범아트가 없으면 이전 음원의 내장 이미지는 제거된다.
- 새로고침과 앱 재시작 후 이미지 출처와 썸네일이 정확히 복원된다.

### 최대 음량

- 50%로 저장한 뒤 새로고침해도 `audio.volume`이 `0.5`로 적용된다.
- 재생 중 50%에서 30%로 낮추면 즉시 `0.3`으로 변경된다.
- 손상되거나 범위를 벗어난 저장값은 기본값으로 복구된다.
- DB 버전 업그레이드 후 기존 슬롯 데이터가 손실되지 않는다.

### 백업과 복원

- 음악·수동 이미지·내장 이미지·최대 음량을 내보낸 뒤 새 테스트 저장소에 가져오면 동일하게 복원된다.
- 빈 슬롯 위치와 `manual`·`embedded` 이미지 출처가 보존된다.
- 한글·공백·특수문자가 포함된 원본 파일명이 왕복 후 유지된다.
- 체크섬 불일치, 잘못된 형식 버전, 누락 파일, ZIP 경로 순회와 압축 폭탄 의심 파일을 거부한다.
- 복원 도중 OPFS 쓰기 또는 IDB 커밋을 강제로 실패시켜도 기존 주크박스가 유지된다.
- 같은 백업을 Android Chrome과 iPhone Safari/PWA에서 가져올 수 있다.
- 백업·복원 기능은 네트워크 연결 없이 작동한다.

### 반복·긴 입력 안정성

- 같은 카드를 1초에 10회 눌러도 첫 번째 재생만 실행된다.
- 여러 카드를 번갈아 빠르게 눌러도 안정화 구간에는 최초 선택 음악만 유지된다.
- 반복 터치가 계속되는 동안 700ms마다 재생·정지가 다시 실행되지 않는다.
- 700ms 이상 입력이 멈춘 뒤 같은 카드를 한 번 누르면 정상적으로 정지한다.
- 카드를 3초 또는 10초 동안 눌러도 손을 뗄 때 한 번만 실행된다.
- 카드 밖으로 이동, `pointercancel`, 다중 터치와 보조 버튼은 재생을 발생시키지 않는다.
- Enter·Space 반복 키 입력은 한 번만 처리하면서 단일 키보드 조작은 정상 동작한다.
- 톱니바퀴는 기존처럼 2초 길게 누른 경우에만 설정을 한 번 연다.
- Android Chrome과 iPhone Safari/PWA 실제 기기에서 동일한 결과를 확인한다.

### 재생 통계

- 재생에 성공한 새 카드 선택은 선택 횟수를 정확히 한 번 올린다.
- 반복 터치, 재생 실패, 같은 카드 정지와 일시정지 후 이어듣기는 선택 횟수를 올리지 않는다.
- 자연스럽게 끝난 곡만 완료 횟수를 올리고 중간 정지·곡 변경은 올리지 않는다.
- 재생·일시정지·백그라운드 재생을 섞어도 누적 청취 시간의 오차가 체크포인트 간격 이내다.
- 사진 교체는 통계를 유지하고 음원 교체·슬롯 삭제는 이전 곡 통계를 분리한다.
- 새로고침과 앱 재시작 후 통계가 복원되며 기존 DB 업그레이드 후 음원·사진·설정이 유지된다.
- 손상된 통계값은 0으로 복구하되 음악 재생은 정상 작동한다.
- 통계 저장 실패를 강제로 발생시켜도 재생이 중단되지 않는다.
- 백업 후 새 저장소에 복원하면 곡별 통계와 `trackId` 연결이 유지된다.
- 사용자용 3×3 화면의 카드 순서와 표시 요소가 통계 때문에 달라지지 않는다.

### 화면 꺼짐 방지 (Screen Wake Lock)

- 음악 재생 시작 시 Wake Lock이 성공적으로 활성화되고, 정지/완료 시 해제된다.
- 탭 전환 후 복귀 시 Wake Lock이 안전하게 재활성화된다.
- 보호자 설정의 '화면 켜짐 유지' 옵션 변경이 영구 저장되고 즉시 반영된다.
- Wake Lock 미지원 브라우저에서도 오류 없이 재생이 지속된다.

### 타일 영역 및 그리드 모드

- 보호자 설정에서 2×2, 2×3, 3×3 모드 선택 시 메인 화면 타일 배치가 즉각 변경된다.
- 2×2 모드에서 4개의 카드가 화면을 가득 채우며 터치 영역이 대폭 확장된다.
- 사용자 플레이 화면에는 어떤 추가 조작 버튼도 노출되지 않는다.

### 다중 화면 (화면 1, 2, 3)

- 보호자 설정에서 화면 1, 2, 3 전환 시 각 화면의 슬롯 데이터가 격리되어 로드된다.
- 화면이 전환되어도 기존 곡의 통계(`trackId`)가 왜곡되지 않고 보존된다.
- 백업 및 복원 시 1~3번 화면 전체가 단일 백업 파일로 왕복 보존된다.

### 기존 UX 비회귀

- 같은 카드를 다시 누르면 정지하고 처음으로 되감긴다.
- 다른 카드를 누르면 이전 음악이 정지하고 새 음악 하나만 재생된다.
- 빈 슬롯은 위치를 유지하지만 사용자에게 보이거나 눌리지 않는다.
- 사용자 화면에는 글자, 음량 조작, 추가 메뉴가 나타나지 않는다.
- 설정은 톱니바퀴를 2초 동안 누른 경우에만 열린다.
- 800×1280과 1280×800에서 3×3 격자가 스크롤 없이 유지된다.

## 7. 완료 및 인계 조건

- 위 테스트 결과를 새 저장소에서 실행한 로그와 함께 남긴다.
- Android Chrome PWA 실제 기기에서 홈 화면 설치, 재부팅 후 복원, 화면 잠금 중 재생, 최대 음량 적용을 확인한다.
- `git diff --check`와 JavaScript 문법 검사를 통과한다.
- 작업 파일과 워크스루를 하나 이상의 명확한 커밋으로 남긴다.
- 브랜치를 원격에 푸시하고 커밋 해시와 남은 한계를 보고한다.
- 테스트 화면 캡처는 저장소 안의 상대경로로 보관하여 GitHub에서도 표시되게 한다.
